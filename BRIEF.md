# Work Commander：独立工作工具包

> 首版历史任务说明，不是现行 skill 加载入口。2026-09-11 起协作使用 `work-commander` / `work-commander-owner`，Git 服务工程使用独立 `service-development`；下文旧 `work-owner` 路径仅保留为历史来源，不再要求读取或恢复。现行入口与兼容边界见 [运行文档](docs/operations.md#协作入口与旧定义退役)。

用户于 2026-09-10 20:49（+08:00）明确授权新 session 完整实施。项目名暂用 Work Commander，不代表增加 Commander agent。

## 目标与分工

交付一个成熟、趁手、可运行的独立工具包：工作服务、持久数据、MCP、协作 skill、专门展示工作的页面。启用 MCP 和 skill 的 agent 能按同一套规范分配、承接、更新和交付工作，为未来大屏 dashboard 提供工作模块。

Cockpit 继续提供会话基础能力；本服务保存任务业务数据、执行显式工作操作。不是第二套 Copilot、聊天系统或自动调度平台，不往 Cockpit 基础 MCP 塞任务语义。

这是一个独立完整目标，由一个新 owner 负责研究、实现、隔离、运行交付及收尾。讨论方不是工程协调或阶段审批中转。

## 先学习再实现

必须先读这些现有材料，理解而非机械复制旧流程：

- `/home/honglai/.copilot/skills/work-owner/SKILL.md` 及 `templates.md`：现行两层工作约定。
- `/home/honglai/weixin-assistant/AGENTS.md`：阿来的入口和权限边界。
- `/home/honglai/weixin-assistant/USER.md`：协作、表达、模型与隔离偏好。
- `/home/honglai/weixin-assistant/guides/memory.md`、`guides/maintenance.md`：渐进式加载和唯一账本。
- `/home/honglai/weixin-assistant/memory/tasks.md`：先读头，再定向看本目标、`cockpit-minimal-native-usage-review`、`session-reasoning-capability-fallback`、`cockpit-actions-release-research`；其余仅按需要抽取案例，不导入全库或重放旧派单。

当前真实痛点：继续一个 unloaded owner 要查状态、恢复、再查模型、投递；只核对模型却返回整个目录及队列正文；派单反复手写绑定和边界；回执靠手工更新 Markdown；旧阶段完成会误盖新授权的未完成工作。研究已完成、后来授权实现仍在途，就是必须正确处理的案例。

无需全量读取私人聊天、数据库或复制所有历史。需要能力事实时按公开 Cockpit schema、安装的 SDK 和当前源码有限查询，不把历史记录当实时状态或新授权。

## 必须保留的协作规则

- 一个独立验收目标一个 owner session；同目标继续原 owner，新目标显式选择新建或真实可用的 fork。不按标题、cwd、空闲状态自动复用。
- 新建/fork 不继承旧执行授权；caller、workstream、目标与授权必须明确绑定。
- 讨论方决定目标/授权/重要取舍，owner 自主完成普通步骤。真正受阻直接向自己 session 的用户提问，不增加协调角色。
- 必要工程隔离由 owner 负责；工作服务不能声称创建 session 就等于隔离文件或数据库。
- 不自动拆任务、续派、催工、广播、补发历史回执或运行模型总结聊天。
- 技能负责指导行为，服务负责能实际校验的输入、归属和状态转换；不能宣称启用 skill 就能强制所有 agent 正确工作。

## 服务、数据和 MCP

实现少量高层语义操作，名称和具体 schema 由 owner 设计：创建/派单、读取任务、报告重要进展/阻塞、交付结果，以及必要的显式目标变更。不要造一个任意命令执行器。

派单输入应表达：新建/fork/继续、目标 session 或 cwd、caller/workstream、模型要求、目标/范围/验收/授权。将必要的恢复、模型核对、投递组合成可追踪操作，不把完整模型目录或所有面板默认返回。新建的工作 session 按用户偏好使用 GPT-6 Astra；忙 session 不得被强制切模/打断。

任务最小数据包含：稳定身份、目标和授权版本、owner/caller、已知状态、重要进展或阻塞、结果入口、更新时间，以及保证投递和并发正确所需的最少操作信息。选型优先小而可靠，避免过早拆多个服务。

任务状态不是 session 忙闲状态。区分记录创建、投递受理/排队、owner 实际承接、结果已报告、目标完整交付；idle 不等于完成。目标扩展或授权变更后，旧版本的完成报告不得覆盖新目标。

必须处理幂等、并发更新、服务重启、部分失败及未知结果：例如已创建 session 但投递失败，返回真实 session ID 和失败步骤；非幂等结果未知时不自动重放。不能宣称一个 MCP call 天然是原子事务，也不能用全历史扫描来核对。

报告来源/任务归属需要实际可信边界，不能仅相信 payload 自称 owner。允许单用户受保护部署，但不要承诺它天然隔离同一机器上的所有 agent。认证、授权、输入校验、密钥处理及可信调用身份需要清楚设计。

只持久化本服务拥有的任务数据和必要操作记录；不保存原生聊天/模型目录/session 状态副本，不轮询采集聊天、不断同步状态或以缓存掩盖原生能力缺口。底层基础能力优先经 Cockpit 已发布入口适配，不另连一套 Copilot runtime 管真实会话。

高层工具既要减少 agent 的 call 次数和返回 token，也要减少底层无用读取。不能只是内部照旧读取一切再截短输出。结果默认简洁、有明确状态和必要 ID；详情按需分页/字段选择。

## 进展、交付和通知

owner 可主动报告承接、重要进展、阻塞和结果，不要求每个工具步骤打点，不编百分比，不因工具服务短暂不可用重复执行实际工作。

报告先落任务记录及页面，不每次唤醒讨论 agent。最终结果或真正需要关注的事件可定向通知绑定 caller；真正决策仍由 owner 在本 session 问用户。只做可追踪的定向投递，不形成 ACK/进度循环；不能同时由 owner 和服务重复发同一最终通知。

MCP/skill 对任务进展报告的规范可以在新工具包内完善，不能把旧 work-owner 的“无过程聊天汇报”误解成禁止服务记录重要进展，也不能反过来让所有进展都变成聊天通知。

## 工作展示页

专门页面与 MCP 使用同一权威任务数据，显示正在做、阻塞、需要用户决定、最近交付；详情展示目标、有限进展及成果入口，可跳到 Cockpit 对应 session。

第一版以清楚、稳定的展示为主，不默认加入拖拽改状态、自动排程或复杂插件平台。采用程序事件更新/必要同步，不靠 agent 定时总结。为后续大屏嵌入留清晰入口，不在本次实现语音、全套自动布局或仪表板插件生态。

工作页面和 API 不得因“展示”就匿名公开私人任务；使用适当的本地/已有受保护访问方式。新增公网域名、认证边界或外部付费依赖需要先询问用户。

## 与现有环境的边界

当前 `tasks.md` 是既有工作的唯一账本；新服务初始使用隔离样例或明确由它管理的新任务。设计可核对、无副作用的迁移方案，但不得批量导入并重新派发旧工作、改变 caller 或通知历史 owner。实际切换账本须先向用户确认，不能长期双写两份真相。

授权包含本项目必要依赖、初始化仓库、服务实现、数据迁移机制、MCP/skill 打包安装说明、工作页、自动化测试及本机安全运行交付。不是只交一个设计或空壳 demo；普通工程步骤自行完成。

新 skill 使用独立名称，保持核心规则一致；不要静默覆盖全局 work-owner、批量启用 MCP/skill、改变其他 session 配置或重启 Cockpit。可以在隔离测试 session 验证启用/发现/协作全链路，正式全局接入或替换现有约定的必要步骤直接与用户确认。

并行中的历史/SSE实现和模型档位修复保持原 owner，本项目不得修改它们的共享代码、部署安排或任务绑定。发现基础能力缺口先说明最小依赖，不私自重构 Cockpit。微信未知发送保持暂停，不发送/重发微信、不交易、不删除重要数据。

## 完成标准

- 服务可可靠启动、停止和恢复，数据重启后保持一致，升级/备份/恢复有清楚方法；不能只用临时开发进程冒称部署完成。
- 真实 MCP 客户端和启用配套 skill 的隔离 agent 能完成显式派单、承接、进展/阻塞、最终交付及按需读取；页面展示同一结果。
- 能说明投递部分失败/未知、重复报告、并发更新、目标变更后的旧报告、服务重启、非法报告归属等场景的实际处理。
- 默认输出精简，报告底层操作和返回体的实测边界，不只写“更高效”。
- 交付必要代码、文档、可运行入口、真实状态、剩余限制及待用户决定事项。不能把已受理/已构建当作完整上线。
- owner 自主完成整个目标；关键产品取舍、新风险、无法可靠满足的能力边界直接用 ask_user 与用户讨论，不堆一份长报告等待重复开工。

本任务 workstream 为 `work-commander-toolkit`。实际 caller 和 owner session ID 以派单为准；本文件不包含其它历史目标的执行授权。

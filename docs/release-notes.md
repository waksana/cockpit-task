# Cockpit Task 0.3.1

Adds caller-scoped idempotency receipts and schema v11 while preserving original
receipt evidence and reserving unattributable legacy request IDs. This package
requires the documented Cockpit host identities and capability versions; merge
does not authorize migration, installation, release or deployment.
